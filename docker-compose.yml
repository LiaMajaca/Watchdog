version: '3.8'

services:
  collector:
    build: ./src/collector
    container_name: collector
    # resource constraints as per README
    deploy:
      resources:
        limits:
          memory: 512M
    # Add other configurations like environment variables, ports, etc.

  inference_engine:
    build: ./src/inference_engine
    container_name: inference_engine
    # resource constraints as per README
    deploy:
      resources:
        limits:
          memory: 512M
    # Add other configurations

  response_agent:
    build: ./src/response_agent
    container_name: response_agent
    # resource constraints as per README
    deploy:
      resources:
        limits:
          memory: 512M
    # Add other configurations

  dashboard:
    build: ./src/dashboard
    container_name: dashboard
    # resource constraints as per README
    deploy:
      resources:
        limits:
          memory: 512M
    ports:
      - "80:80" # Example port mapping for a web dashboard
    # Add other configurations